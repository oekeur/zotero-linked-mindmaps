/tmp
/out-tsc

# separate nested git repo for PM (Backlog.md), never published with source
# no trailing slash: matches the symlink worktrees use to reach the shared checkout, not just a real dir
/project

# vendored third-party reference docs, not project source
/zotero-plugin-docs
/zotero-plugin-toolkit-docs

# scaffold's template example code, kept as a reference, not project source
/zotero-plugin-template-examples

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml
/.pnp
.pnp.js

# zotero-plugin-template / scaffold
.DS_Store
.env
.scaffold
build
logs
tsconfig.tsbuildinfo

# JetBrains personal IDE state, not shared project config
/.idea

# transient Claude Code worktree checkouts; settings.json and hooks/ stay
# tracked so the pre-merge test gate travels with the repo
/.claude/worktrees
